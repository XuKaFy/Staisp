#include "evaluate.h"

Reg evaluate(Chunk &c, pNode n, Regs &regs)
{
    return regs.pc;
    /*
    switch(n->type) {
    case NODE_IMM: {
        return ;
    }
    case NODE_SYM: {
        return ;
    }
    case NODE_OPR: {
        return ;
    }
    }*/
}
